var arr = [1,2,3];

//create a function named 'first' that returns the first object from a given array
//alert the result of your function

function first(yourArray) {
	return yourArray[0];
}

alert(first(arr));
